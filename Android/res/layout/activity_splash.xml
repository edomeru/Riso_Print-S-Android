<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <ImageView
        android:id="@+id/risoLogoImageView"
        android:layout_width="@dimen/temp_splash_logo_width"
        android:layout_height="@dimen/temp_splash_logo_height"
        android:layout_gravity="center"
        android:background="@color/bg_color_splash"
        android:contentDescription="@string/default_content_description"
        android:src="@drawable/img_riso_splash" />

</FrameLayout>
